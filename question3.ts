

// Question 3 Code: What is the output in the console when this code is executed?
// Assume TypeScript

printList(list: string[]): int {
  for (let i = 0; i < list.length; i++) {
    console.log(listEl);
  }
  return list.length;
}

printList(['hello', 'my', 'friend']);
printList([1, 2, 3]);



